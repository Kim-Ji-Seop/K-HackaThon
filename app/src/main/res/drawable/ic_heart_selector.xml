<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 선택되었을 때의 상태 -->
    <item android:drawable="@drawable/ic_heart_selected" android:state_selected="true"/>
    <item android:drawable="@drawable/ic_heart_selected" android:state_checked="true"/>
    <item android:drawable="@drawable/ic_heart_selected" android:state_pressed="true"/>

    <!-- 기본 상태 (선택되지 않았을 때) -->
    <item android:drawable="@drawable/ic_heart_no_select"/>
</selector>